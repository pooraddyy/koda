---
description: Check changed user-facing text for spelling, clarity, and terminal accuracy
---

Inspect the requested Markdown, CLI messages, help text, and comments for spelling,
grammar, inconsistent terminology, broken links, stale command names, and claims
that the current code cannot support. Preserve code identifiers, command syntax,
and intentionally technical terms.

Return proposed edits with path and surrounding context. Do not silently rewrite
files or modify generated content. Flag ambiguous product or security claims for a
maintainer decision rather than guessing.
