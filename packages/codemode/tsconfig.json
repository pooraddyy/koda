{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "@tsconfig/bun/tsconfig.json",
  "compilerOptions": {
    "noUncheckedIndexedAccess": false,
    "types": ["bun"],
    "lib": ["ESNext", "DOM", "DOM.Iterable"] // koda_change - align upstream's new package with koda's tsgo environment
  }
}
