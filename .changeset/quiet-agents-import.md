---
"@koda-code/cli": patch
"koda-code": patch
---

Add clean-room compatibility for agent Markdown in `.pi/agents` and `.agents/agents`, normalize common external frontmatter aliases, and enforce imported nested-agent allowlists through Koda’s existing Task runtime.
